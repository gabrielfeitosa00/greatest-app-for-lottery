import React from 'react';

const Toolbar = (props)=>{
    return(
        <header>
            <div>
                TGL
            </div>
            <div>
                {/* NavItems here */}
            </div>
        </header>
    )
}

export default Toolbar;