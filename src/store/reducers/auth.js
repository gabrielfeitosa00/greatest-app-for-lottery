const initialState = {
    userName:'',
    password:'',
    email:'',
    isAuth:true
}

const reducer = (state=initialState,action)=>{
    switch (action.type) {
        default:
            return state;
    }
}


export default reducer;